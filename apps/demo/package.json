{
  "name": "@greenways/hodos-demo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/main.js --bundle --format=esm --platform=browser --target=es2022 --loader:.hal=text --loader:.png=file --external:node:worker_threads --entry-names=hodos --asset-names=hodos --outdir=dist && cp index.html dist/index.html"
  }
}
