# Visible session console

This story exercises the trusted REPL host used by the Hara Playground.

The host projects session status, namespace, transcript and input state and
emits submit, cancel, history, clear and inspect-value requests. Runtime
transport, execution, retained values and history policy remain product
services.
