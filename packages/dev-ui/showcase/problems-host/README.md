# Visible Problems host

This complete project mounts the trusted Dev UI Problems adapter over bounded
structured diagnostics.

The visible host may filter, select, copy and request source navigation. It does
not produce diagnostics, mutate source or choose how an editor responds to the
request.
