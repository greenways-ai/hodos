# Visible activity Catalog

This complete project mounts the trusted Dev UI Catalog adapter over the same
serializable model published by `@greenways/hodos-dev`.

The host can update visible selection and check evidence and can emit only the
declared semantic events. Source insertion, activity opening and check execution
remain injected application services.
