# Visible project Explorer

This story exercises the trusted Explorer host used by the Hara Playground.

The host receives a serializable `hodos.dev/explorer` model, updates visible
file-tree mechanics and emits declared semantic events. It does not become the
source authority. Replacing the area disposes listeners, observers and owned
DOM.
