# Visible Value Inspector

This complete project mounts the trusted Dev UI Value Inspector adapter over a
bounded retained map.

Tree expansion, path selection, copy, refresh and close are semantic events.
The application still owns the retained runtime value and decides whether any
request is permitted.
