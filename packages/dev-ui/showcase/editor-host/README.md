# Visible Hara editor

This story exercises the trusted Editor host used by the Hara Playground.

The adapter mounts the product editor into the Hodos Workspace area, forwards
serializable model updates and routes only declared editor events. The
application remains authoritative for source text, saves, evaluation and
commands; the host owns input and visible editor mechanics.
