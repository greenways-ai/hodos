# @greenways/hodos-ui-world-publication

Optional draft review and publication UI for Hodos. It visualises semantic
change sets, accepts bounded draft imports, and dispatches repository or Hestia
publication intents while leaving storage and transport implementations to the
host.

The add-on requires `publication.intent` and `workspace.drafts` and contributes
`world.ui/publication`.
