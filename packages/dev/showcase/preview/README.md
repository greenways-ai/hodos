# Render an interface value

This complete project is the Canvas for the `Preview` package story.

`src/main.hal` evaluates to ordinary Hara tree data. The Playground's injected
preview service renders it inside the `preview` surface. The Hodos Dev model
describes status, theme and output; sandbox policy and executable rendering are
not encoded in package metadata.
