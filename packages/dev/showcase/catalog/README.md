# Browse a bounded activity Catalog

This complete project mounts the package-owned `hodos.dev/catalog` component
with descriptive toolsets, one selected activity and bounded check evidence.

The model deliberately excludes executable source and check expressions. User
interactions emit only the declared Catalog semantic events; insertion, opening,
checking and reset behavior remain application policy.
