# Inspect source-linked Problems

This complete project mounts `hodos.dev/problems` with one retained runtime
warning and one selected compiler error.

The component can filter, select, copy and request source navigation, but it
never edits source or owns diagnostic production. Exact ranges, counts and
metadata remain bounded serializable application evidence.
