# @greenways/hodos-viewer

The browser projection for Hodos Worlds. It contributes a `viewer/worlds`
factory to Hodos Core and keeps rendering concerns—PlayCanvas, world manifests,
editor panels, and browser surfaces—outside the semantic kernel.

Subpath exports expose authoring, editor, draft, drag, and GitHub-world modules
without requiring consumers to import repository-relative source paths.
