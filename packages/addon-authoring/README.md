# @greenways/hodos-addon-authoring

The semantic authoring session: multi-selection, transforms, collections,
assets, prefabs, animation, scripts, history, and publication commands. UI and
rendering remain separate projections supplied by viewer add-ons.
